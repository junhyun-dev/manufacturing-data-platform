# Base dependencies, including the retained catalog/lakehouse tests.
# requirements-dev.lock pins these plus the current OPC UA runtime.
fastapi
uvicorn
pymongo

# dev / test
mongomock
pytest
httpx        # FastAPI TestClient
